#include "main.h"

/**
  * _isupper- value c
  * @c: value taken by main
  * Return: 1for upper, 0 otherwise
  */

int _isupper(int c)
{
	if (c <= 90 && c >= 65)
	{return (1);
	}
	else
		return (0);
}
