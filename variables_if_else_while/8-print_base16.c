#include<stdio.h>

/**
 * main - prints numbers of base 16
 * Return: 0
 */

int main(void)
{
	char i;
	char b;

	for (i = '0'; i <= '9'; i++)
	{
		putchar(i);
	}
	for (b = 'a'; b <= 'f'; b++)
	{
		putchar('\n')
	}
		return (0);
}
