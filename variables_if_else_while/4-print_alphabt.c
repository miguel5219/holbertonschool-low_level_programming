#include<stdio.h>

/**
 * main - except q and e
 * Return: 0
 */

int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	for (i = 'q'; i = 'e');
	{
		putchar(0);
	}
	putchar('\n');
	return (0);
}
