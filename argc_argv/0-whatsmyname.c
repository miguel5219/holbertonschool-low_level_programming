#include "main.h"

/**
  * main - print name
  * @argc: size of argv
  * @argv: all need for the program
  * Return: 0
  */

int main(int argc, char *argv[])
{
	if (argc > 0)
	printf("%s\n", argv[0]);

	return (0);
}
