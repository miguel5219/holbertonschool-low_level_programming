#include "main.h"
/**
 *main - check the code
 *
 *Return: 0
 */

int _isalpha(int c)
{
	char letter;
	if( letter = 'a'; letter <= 'z'; letter++ )
	{
		return (0);
	}
	else (letter == 'c');
	{
		return (1)
	}
}
